{
  "name": "@family-expense-tracker/functions",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "seed": "npm run build && node lib/seed.js"
  },
  "dependencies": {
    "@family-expense-tracker/shared": "file:vendor/shared",
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  }
}
